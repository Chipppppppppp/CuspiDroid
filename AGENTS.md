# AGENTS.md

このリポジトリで作業する Codex（GPT-6 Astra を含む）向けの指示です。

## 作業の進め方と完了条件

- ユーザーの依頼を実装・検証・コミットまで進めること。通常の実装判断は既存コードと依頼の文脈から決め、計画の提示や確認待ちだけで止めないこと。確認が必要なのは、手元で解決できない不足情報や、依頼の範囲を超える破壊的・外部公開操作がある場合に限る。
- 作業前に `git status --short` と関連コードを確認し、ユーザーの未コミット変更を保持すること。不具合は原因と呼び出し元を調べ、関連する経路をまとめて直すこと。無関係なリファクタリングは行わない。
- 修正内容に応じた回帰テストを追加・実行すること。UIの表示タイミングの不具合は、可能なら端末またはエミュレーターで確認し、確認できなかった範囲は報告すること。
- 変更が揃った時点で、コミット前に必ず `.\gradlew.bat assembleDebug` を実行し、成功を確認すること。その後に変更した場合は再実行する。失敗した場合は原因を直し、未検証のまま成功扱いにしない。
- 最終差分を確認して今回の変更を git にコミットすること。変更のない調査・説明では空コミットは不要。最終報告は日本語で、修正結果・検証結果・未確認事項・コミットIDを簡潔に記載すること。
- push・GitHubリリースは依頼された場合に行うこと。リリース時は、ひとつ前のリリース時点の `README.md` と現在の `README.md` を比較し、追加された機能をリリース説明に記載する。配布物の SHA-256 添付は不要。

## 変更に伴う同期

- 利用者向け機能を追加・変更した場合は、日本語版 `README.md` と英語版 `README.en.md` を同期して更新すること。既存の説明どおりの動作へ戻す不具合修正では、変更履歴をREADMEに追記する必要はない。
- `BbsLinksActivity.RECOMMENDED_CUSTOM_BBS_LINKS` の「対応BBSを一括追加」対象と、`README.md` および `README.en.md` のカスタムBBS一覧は、名前・URL・並び順を常に同期すること。
- 設定や永続化データを追加・変更した場合は、CuspiDroidバックアップの作成・復元対象に含まれることを確認すること。SharedPreferences以外へ保存するデータはバックアップへ明示的に追加し、必要に応じてバックアップ形式のバージョンも更新すること。

## READMEの記述方針

- `README.md` と `README.en.md` は、利用者が現在利用できる機能の概要と、インストール・ビルドに必要な情報だけを簡潔に記載すること。
- 「機能」／「Features」は原則10項目以内とし、1項目を1文で記載すること。詳細を追加するときは既存の分類へ統合し、変更履歴のように項目を継ぎ足さないこと。
- 内部実装、性能改善の手法、アルゴリズムの判定条件、例外処理、細かなアニメーションや配色、個別の変更経緯はREADMEに記載しないこと。必要な場合はコードコメント、開発者向け文書、GitHubリリースの説明へ記載すること。
- 日本語版と英語版は、見出し・項目数・並び順・情報の粒度を同期すること。更新後は両方を読み直し、同じ内容が簡潔に表現されていることを確認すること。

## UIテーマの参照方法

- UIの色は `Theme` を唯一の参照元とし、画面クラスへ `Color.rgb(...)` や色リテラルを追加しないこと。役割に応じて `background`（画面）、`topBar`（上下バー）、`surface`（カード・パネル・メニュー）、`post`（投稿）、`unread`（未読投稿）、`field`（入力欄）、`text`（本文）、`muted` / `subtle`（補助表示）、`border` / `strongBorder`（境界線）、`active` / `searchHighlight`（選択・ページ内検索強調）、`accent`（操作・アイコン）を使うこと。優先ワードには独立したテーマ色を設けず、文字色を変えずに `searchHighlight` で太めの下線を引き、ページ内検索の背景ハイライトと形状を区別すること。同じ文字範囲へページ内検索が一致した場合は、下線を維持したまま検索背景も重ねて表示すること。スクロールサイドバー本体は `border`、つまみと自分の投稿バーは `accent`、未読範囲は `sidebarUnread`、自分への返信バーは `replyPostMarker`、ツリー線は `strongBorder` を参照すること。順位・勢い・レス数など値の強さを補間する表示には `metricLow` / `metricHigh` を使うこと。
- `Theme.background(context)` のような通常のアクセサーを使うこと。`MainActivity` では既存の `bgColor()`、`surfaceColor()` などのラッパーを優先すること。通常・プライベートは常に同じパレットを使い、プライベート状態を理由に画面全体の色を分岐させないこと。プライベート状態は画面最上部の専用バーで表し、その塗り色には `accent`、文字・アイコンには `Theme.contrastingText(...)` を使うこと。
- システムバーには `Theme.applySystemBars(...)`、ダイアログには `Theme.styleDialog(...)` / `Theme.stylePopupDialog(...)`、チェックボックスとラジオボタンには `Theme.tintCompoundButton(...)`、ローディング用の `ProgressBar` には `Theme.tintProgressBar(...)` を使い、選択中のパレットと明暗設定を反映すること。
- `accent` を塗り色として使うボタンやバッジの前景色は、任意のカスタム色でもコントラストを保つため `Theme.contrastingText(...)` で決めること。
- 新しいセマンティック色を追加する場合は、まず既存色へ統合できないか確認すること。独立色が必要な場合は、ティール・グリーン・ブルー・スカイブルー・インディゴ・パープル・クリムゾン・ピンク・ピュアホワイト・ピュアダークの全組み込みパレット、`Theme.Palette` のJSON入出力、`Theme.COLOR_KEYS`（`Theme.EDITABLE_COLOR_KEYS` と同一）、テーマ編集画面の表示名・GUIカラーピッカー・色プレビューを同時に更新し、テーマ形式のバージョンも更新すること。実際に参照しない互換専用フィールドは残さないこと。
- ランチャーアイコンは `ic_launcher_foreground.xml` を多色版の正本とすること。変更時は `ic_launcher_monochrome.xml` の輪郭も同期し、通常・円形それぞれの旧Android用アイコン、アダプティブアイコン、Android 13以降のモノクロテーマアイコンをすべて維持すること。READMEが参照する `docs/icon.svg` は `syncReadmeIcon` タスクが正本と背景色から自動生成するため直接編集しないこと。モノクロテーマアイコンの色はアプリ内の `Theme` ではなく端末の壁紙・システムテーマとランチャーが決めるため、アプリ内テーマ色との連動処理は追加しないこと。
